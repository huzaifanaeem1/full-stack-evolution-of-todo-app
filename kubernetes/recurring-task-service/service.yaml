# T062: Recurring Task Service Kubernetes Service
# Phase V - Part B: Event-Driven Architecture with Kafka and Dapr

apiVersion: v1
kind: Service
metadata:
  name: recurring-task-service
  namespace: default
  labels:
    app: recurring-task-service
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: recurring-task-service
